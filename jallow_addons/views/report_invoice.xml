<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="sale_invoice_inherit" inherit_id="account.report_invoice_document">

        <xpath expr='//div[@name="origin"]' position="replace">
        </xpath>
        <!-- sims -->
        <xpath expr='//table[@name="invoice_line_table"]' position="replace">
            <table class="o_has_total_table table o_main_table table-borderless" name="invoice_line_table">
                <thead>
                    <tr>
                        <th name="th_description" class="text-start"><span>Produit</span></th>
                        <th name="th_quantity" class="text-center"><span>Qté</span></th>
                        <th name="th_uom" class="text-center"><span>Unité</span></th>
                        <th name="th_priceunit" t-attf-class="text-center text-nowrap {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}"><span>P.U HT</span></th>
                        <th name="th_priceunit_ttc" t-attf-class="text-center text-nowrap {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}"><span>P.U TTC</span></th>
                        <th name="th_discount" t-if="display_discount" t-attf-class="text-center {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                            <span>Rem.%</span>
                        </th>
                        <!-- <th name="th_taxes" t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}"><span>Taxes</span></th> -->
                        <th name="th_subtotal" class="text-end">
                            <span>Mont. TTC</span>
                        </th>
                    </tr>
                </thead>
                <tbody class="invoice_tbody">
                    <t t-set="current_subtotal" t-value="0"/>
                    <t t-set="current_total" t-value="0"/>
                    <t t-set="lines" t-value="o.invoice_line_ids.sorted(key=lambda l: (-l.sequence, l.date, l.move_name, -l.id), reverse=True)"/>

                    <t t-foreach="lines" t-as="line">
                        <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>
                        <t t-set="current_total" t-value="current_total + line.price_total"/>

                        <tr t-att-class="'fw-bold o_line_section' if line.display_type == 'line_section' else 'fst-italic o_line_note' if line.display_type == 'line_note' else ''">
                            <t t-if="line.display_type == 'product'" name="account_invoice_line_accountable">
                                <td name="account_invoice_line_name">
                                    <span t-if="line.name" t-field="line.name" t-options="{'widget': 'text'}">Bacon Burger</span>
                                </td>

                                <td name="td_quantity" class="text-center text-nowrap">
                                    <span t-field="line.quantity">3.00</span>
                                </td>
                                <td name="td_uom" class="text-center text-nowrap">
                                    <t t-if="line.product_uom_id.id == 1">
                                        <span t-field="line.product_uom_id"  groups="uom.group_uom">carton</span>
                                    </t>
                                    <t t-else="">douzaine</t>
                                </td>
                                <td name="td_price_unit" t-attf-class="text-center {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                    <span class="text-nowrap" t-field="line.price_unit">9.00</span>
                                </td>
                                <td name="td_price_unit_ttc" t-attf-class="text-center {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                    <!-- <span class="text-nowrap" t-field="line.price_subtotal * line.quantity">9.00</span> -->
                                    <span class="text-nowrap" t-esc="'{0:.2f}'.format(line.price_unit * (1 + (line.product_id.taxes_id.amount if line.product_id.taxes_id else 0) / 100))">9.00</span>
                                </td>
                                <td name="td_discount" t-if="display_discount" t-attf-class="text-center {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                    <span class="text-nowrap" t-field="line.discount">0</span>
                                </td>
                                <td name="td_subtotal" class="text-end o_price_total">
                                    <span class="text-nowrap" t-field="line.price_total">27.00</span>
                                    <!-- <span class="text-nowrap" t-field="line.price_subtotal">27.00</span> -->
                                    <!-- <span class="text-nowrap" t-out="round(line.price_subtotal + (18 * line.price_subtotal / 100),2)" t-options='{"widget": "monetary", "display_currency": o.currency_id, "digits": [9, 2]}'>27.05</span> -->
                                </td>
                            </t>
                            <t t-elif="line.display_type == 'line_section'">
                                <td colspan="99">
                                    <span t-field="line.name" t-options="{'widget': 'text'}">A section title</span>
                                </td>
                                <t t-set="current_section" t-value="line"/>
                                <t t-set="current_subtotal" t-value="0"/>
                            </t>
                            <t t-elif="line.display_type == 'line_note'">
                                <td colspan="99">
                                    <span t-field="line.name" t-options="{'widget': 'text'}">A note, whose content usually applies to the section or product above.</span>
                                </td>
                            </t>
                        </tr>

                        <t t-if="current_section and (line_last or lines[line_index+1].display_type == 'line_section')">
                            <tr class="is-subtotal text-end">
                                <td colspan="99">
                                    <strong class="mr16">Subtotal</strong>
                                    <span
                                        t-out="current_subtotal"
                                        t-options='{"widget": "monetary", "display_currency": o.currency_id}'
                                    >31.05</span>
                                </td>
                            </tr>
                        </t>
                    </t>
                </tbody>
            </table>
        </xpath>

        <xpath expr='//p[@name="payment_communication"]' position="replace">
        </xpath>

    </template>
</odoo>