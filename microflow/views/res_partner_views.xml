<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ═══════════════════════════════════════════════════════════════════
         Héritage du formulaire COMPLET (menu Contacts / page dédiée)
    ═══════════════════════════════════════════════════════════════════════ -->
    <record id="view_partner_form_microflow" model="ir.ui.view">
        <field name="name">res.partner.form.microflow</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <!-- Bloc MICRO FLOW inséré après category_id (groupe de droite) -->
            <xpath expr="//sheet/group/group/div[@class='o_address_format']" position="after">
                <group string="Informations MICRO FLOW">
                    <field name="member_id" readonly="1"/>
                    <field name="entity_type"/>
                    <field name="member_type"/>
                    <field name="birthdate"/>
                    <field name="zone_id"/>
                    <field name="gps_coordinates" readonly="1"/>
                </group>
                <div class="mb-3">
                    <button name="action_capture_gps"
                            string="Capturer la localisation GPS"
                            type="object"
                            class="btn btn-secondary"
                            icon="fa-map-marker"/>
                </div>
            </xpath>

            <!-- Champs masqués — non pertinents pour le workflow microfinance -->
            <xpath expr="//field[@name='street2']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='zip']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='website']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='title']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='function']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='category_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='vat']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='state_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='lang']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

        </field>
    </record>

    <!-- ═══════════════════════════════════════════════════════════════════
         Héritage du formulaire SIMPLIFIÉ (dialog quick-create depuis
         un Many2one partner : saisie cycle, crédit, transaction…)
    ═══════════════════════════════════════════════════════════════════════ -->
    <record id="view_partner_simple_form_microflow" model="ir.ui.view">
        <field name="name">res.partner.simplified.form.microflow</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_simple_form"/>
        <field name="arch" type="xml">
            <!-- Odoo 18 place déjà phone/mobile/email sur des lignes séparées
                 (itemSpan=2 = label+valeur = maxCols=2) — aucune modification requise.
                 Le CSS microflow_mobile.scss gère l'empilement sur mobile. -->
            <xpath expr="//field[@name='email']" position="attributes">
                <attribute name="placeholder">Ex : client@exemple.com</attribute>
            </xpath>
        </field>
    </record>

</odoo>
